sip_invite_created.yaml
    via predict
    contact predict
    rtp predict
    rtcp predict

    check:
        creation
            case 12, new nat created
        DIP natref, add/clear
        appinfo2ip add/clear



sip_invite_converted.yaml
    via predict
    contact predict
    rtp predict
    rtcp predict

    check:
        conversion
        rtp predicts:
            case 1, src_mapped
        sip
            case 4, because the nat already done in predict



sip_invite_converted.errors.yaml
----------------------------------------------------------------------
1. call-id in 200 ok does not match call-id in invite
   this tests, no new transaction created for stray response
2. PRACK has no terminating CRLF which tests malformed sip error
3. SDP answer cannot be matched with SDP offer, tests unmerged rtp


